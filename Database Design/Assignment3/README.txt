A web application ( on local host) => Search queries from an existant Database, and do some other stuff related to said database.

Unfortunately, I have lost the files given to us(by the instructor), so it is incomplete on its own.