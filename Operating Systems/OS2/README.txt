Learnig about IPC(inter-process-communication) , with fork/exec and PIPES!

Searching a binary file, by creating a "binary search tree of processes" (using fork() and execlp() )

The leaves are the ones who search. The nodes forward the information they get, towards the root, with the usage of Pipes!

After that we sort our result , using fork/execlp( sort ) (linux)